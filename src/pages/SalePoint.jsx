import { SellpointHeader } from "../components/layout/Header";
import SalePointMain from "../components/sales/SalePointMain";

const SalePoint = () => {
    return (
        <section className="SalePoint">
            <SellpointHeader />
            <SalePointMain />
        </section>
    );
    };

export default SalePoint;